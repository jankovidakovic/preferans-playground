[project]
name = "pref"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jankovidakovic", email = "janko@tensorpix.ai" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
pref = "pref:main"

[build-system]
requires = ["uv_build>=0.8.11,<0.9.0"]
build-backend = "uv_build"
